% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz-comparisons.R
\name{plot_comparisons}
\alias{plot_comparisons}
\title{Plot Average Comparisons}
\usage{
plot_comparisons(
  x,
  x_var = NULL,
  color_var = NULL,
  linetype_var = NULL,
  facet_var = NULL,
  geom = c("auto", "line", "point"),
  estimate_var = "estimate",
  show_uncertainty = TRUE,
  ribbon_alpha = 0.12,
  point_size = 2,
  line_width = 0.7
)
}
\arguments{
\item{x}{A \code{markov_avg_comparisons} object returned by
\code{\link[=avg_comparisons]{avg_comparisons()}}, optionally after \code{\link[=inferences]{inferences()}}.}

\item{x_var}{Character string or \code{NULL}. X-axis variable. If \code{NULL}, uses
\code{"time"} when available, then \code{"state_set"} when available, and otherwise
\code{"contrast"}.}

\item{color_var}{Character string or \code{NULL}. Optional variable mapped to
color. If \code{NULL}, a useful grouping variable is inferred when needed.}

\item{linetype_var}{Character string or \code{NULL}. Optional variable mapped to
linetype for line plots.}

\item{facet_var}{Character vector or \code{NULL}. Optional faceting variable. Use
a length-two vector for \code{facet_grid(row ~ column)}.}

\item{geom}{Character string. \code{"auto"} uses lines when \code{x_var} is \code{"time"}
and points otherwise. Use \code{"line"} or \code{"point"} to override.}

\item{estimate_var}{Character string. Column containing comparison estimates.}

\item{show_uncertainty}{Logical. If \code{TRUE}, add confidence ribbons for line
plots or intervals for point plots when \code{conf.low} and \code{conf.high} exist.}

\item{ribbon_alpha}{Numeric alpha level for confidence ribbons.}

\item{point_size}{Numeric point size for point plots.}

\item{line_width}{Numeric line width for lines and interval geoms.}
}
\value{
A ggplot object.
}
\description{
Plots output from \code{\link[=avg_comparisons]{avg_comparisons()}} on the comparison scale.
}
\details{
The y axis is the contrast scale: difference contrasts are centered on a
dashed reference line at 0, and ratio contrasts are centered on a dashed
reference line at 1. A single plot must contain only one comparison scale.
}
\examples{
\dontrun{
cmp <- avg_comparisons(
  fit,
  variables = list(tx = c(0, 1)),
  estimand = "time_in_state",
  state_sets = c(1, 2),
  times = 1:30
) |>
  inferences(method = "mvn", n_draws = 500)

plot_comparisons(cmp)
}

}
