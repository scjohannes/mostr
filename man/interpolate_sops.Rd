% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sops-interpolate.R
\name{interpolate_sops}
\alias{interpolate_sops}
\title{Interpolate SOPs from Visit Time to Real Time}
\usage{
interpolate_sops(
  x,
  time_map,
  target_times = NULL,
  baseline_time = 0,
  normalize = TRUE
)
}
\arguments{
\item{x}{A \code{markov_sops} or \code{markov_avg_sops} object.}

\item{time_map}{A named numeric vector mapping visit labels to real times, or
a data frame with visit and real-time columns. Data frames may use columns
\code{visit} and \code{real_time}, \code{visit} and \code{time}, or their first two columns.}

\item{target_times}{Optional numeric real-time grid. If \code{NULL}, uses the
mapped visit times plus \code{baseline_time} when the baseline anchor is enabled.}

\item{baseline_time}{Real time of the observed baseline state. The default
\code{0} adds a fixed anchor from the stored previous-state values. Set to \code{NULL}
to disable baseline anchoring.}

\item{normalize}{Logical. If \code{TRUE}, normalize interpolated estimates so
state probabilities sum to one within each time and group.}
}
\value{
An interpolated data frame with the same core columns as \code{x}, where
\code{time} is on the real-time scale.
}
\description{
Maps visit-scale SOP output from \code{\link[=sops]{sops()}} or \code{\link[=avg_sops]{avg_sops()}} to a real elapsed
time scale and linearly interpolates probabilities within patient, state,
draw, treatment, and strata groups.
}
\details{
The recommended workflow for irregular assessment schedules is:
\enumerate{
\item Recode assessment days to visit indices, for example day 3, 7, 14, and 28
to visit \code{1:4}.
\item Fit the Markov model with the visit index as a factor.
\item Compute visit-scale SOPs with \code{\link[=sops]{sops()}} or \code{\link[=avg_sops]{avg_sops()}}.
\item Use \code{interpolate_sops()} or \code{time_in_state(..., time_map = ...)} for
real-day summaries.
}

With RCT standardization from \code{\link[=avg_sops]{avg_sops()}}, the empirical baseline anchor is
shared across treatment counterfactual groups.

If \code{x} contains stored simulation, bootstrap, or posterior draws, the draws
are interpolated too. Interval columns are then recomputed from the
interpolated draws. Coefficient-only draws treat the empirical baseline anchor
as fixed; empirical score-bootstrap, FWB, and refit-bootstrap draws use the
corresponding draw-specific patient weights or resampled cohort.
If interval columns are present but stored draws are unavailable,
\code{interpolate_sops()} warns and falls back to interpolating interval endpoints.
Each numeric series is interpolated only within its own finite source-time
support. Targets before its first or after its last available value return
\code{NA}; the function never extrapolates a series from another group's wider
support.
}
\examples{
\dontrun{
avg <- avg_sops(
  fit,
  newdata = baseline,
  variables = list(tx = c(0, 1)),
  times = NULL
)
interpolate_sops(
  avg,
  time_map = c("1" = 3, "2" = 7, "3" = 14, "4" = 28),
  target_times = 0:28,
  baseline_time = 0
)
}

}
