% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sops-score-bootstrap.R
\name{generate_score_bootstrap_draws}
\alias{generate_score_bootstrap_draws}
\title{Generate One-Step Score-Bootstrap Draws}
\usage{
generate_score_bootstrap_draws(
  model,
  baseline_data,
  id_var,
  n_draws,
  score_weight_dist = "exponential",
  cluster = NULL,
  return_baseline_weights = TRUE
)
}
\arguments{
\item{model}{A \code{robcov_vglm} object with stored \code{scores}, \code{bread}, and
\code{cluster} components, or an \code{orm} object fitted with \verb{x = TRUE, y = TRUE}.
For \code{orm}, \code{cluster} must be supplied.}

\item{baseline_data}{Optional baseline data (one row per patient) used only
when patient-level averaging weights should be returned.}

\item{id_var}{Name of patient ID variable in \code{baseline_data}.}

\item{n_draws}{Number of simulation draws.}

\item{score_weight_dist}{Cluster weight distribution. Currently only
\code{"exponential"} is supported.}

\item{cluster}{Optional row-level cluster vector for \code{orm} models.}

\item{return_baseline_weights}{Logical. If \code{TRUE}, return normalized
patient-level averaging weights aligned to \code{baseline_data}. If \code{FALSE},
only coefficient perturbations are generated.}
}
\value{
A list with:
\itemize{
\item \code{beta_draws}: Matrix \verb{[n_draws x p]} of perturbed coefficients.
\item \code{baseline_weights}: Matrix \verb{[n_draws x n_patients]} of normalized
patient averaging weights.
}
}
\description{
Computes simulation draws using cluster-level score perturbations and a
one-step Newton approximation. Also returns patient-level averaging weights
(shared with score perturbations) for \code{avg_sops} marginalization.
}
\keyword{internal}
