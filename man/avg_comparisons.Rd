% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sops-comparisons.R
\name{avg_comparisons}
\alias{avg_comparisons}
\title{Average Comparisons From Markov State Occupancy Probabilities}
\usage{
avg_comparisons(
  model,
  newdata = NULL,
  variables,
  estimand = c("sop", "time_in_state", "time_benefit"),
  state_sets = NULL,
  comparison = c("difference", "ratio"),
  by = NULL,
  times,
  y_levels = NULL,
  absorb = NULL,
  time_map = NULL,
  baseline_time = 0,
  target_times = NULL,
  time_unit = NULL,
  refit_data = NULL,
  id_var = NULL,
  time_var = "time",
  p_var = "yprev",
  p2_var = NULL,
  gap_var = NULL,
  time_covariates = NULL,
  include_re = FALSE,
  n_draws = 100L,
  seed = NULL,
  posterior_summary = c("mean", "median"),
  conf_level = 0.95,
  return_draws = FALSE,
  ...
)
}
\arguments{
\item{model}{A fitted Markov transition model supported by \code{\link[=avg_sops]{avg_sops()}}.}

\item{newdata}{Optional data frame of standardization profiles. See
\code{\link[=avg_sops]{avg_sops()}}.}

\item{variables}{A named list with one counterfactual variable. The variable
must contain at least two values, for example \code{list(tx = c(0, 1))}.}

\item{estimand}{Character scalar. One of \code{"sop"}, \code{"time_in_state"}, or
\code{"time_benefit"}.}

\item{state_sets}{State selection for \code{"sop"} and \code{"time_in_state"}. \code{NULL}
returns one result per state. An atomic vector is treated as one lumped
state set. A named list returns one result per named state set.}

\item{comparison}{Character scalar. \code{"difference"} computes comparison level
minus reference level. \code{"ratio"} computes comparison level divided by
reference level. \code{"time_benefit"} only supports \code{"difference"}.}

\item{by}{Optional character vector of variables to stratify by after
standardization.}

\item{times}{Required visit-scale time points. See \code{\link[=avg_sops]{avg_sops()}}.}

\item{y_levels}{A vector of state levels. See \code{\link[=avg_sops]{avg_sops()}}.}

\item{absorb}{The absorbing state. See \code{\link[=avg_sops]{avg_sops()}}.}

\item{time_map}{Optional named numeric vector or data frame mapping visit
labels to real elapsed times. Used by \code{"time_in_state"} and
\code{"time_benefit"}.}

\item{baseline_time}{Real time of the observed baseline state used as an
interpolation anchor, or \code{NULL} to disable baseline anchoring. See
\code{\link[=interpolate_sops]{interpolate_sops()}}.}

\item{target_times}{Optional numeric real-time grid when \code{time_map} is
supplied. When omitted, real-time comparisons use the mapped modeled
follow-up times and exclude the baseline interval.}

\item{time_unit}{Optional label stored in output.}

\item{refit_data}{Optional full longitudinal data used only by refit-bootstrap
inference. It is not used for point estimates. See \code{\link[=avg_sops]{avg_sops()}}.}

\item{id_var}{Name of the patient ID variable. See \code{\link[=avg_sops]{avg_sops()}}.}

\item{time_var}{Name of the time variable in the model. See \code{\link[=avg_sops]{avg_sops()}}.}

\item{p_var}{Name of the previous state variable in the model. See
\code{\link[=avg_sops]{avg_sops()}}.}

\item{p2_var}{Optional second previous-state variable. See \code{\link[=avg_sops]{avg_sops()}}.}

\item{gap_var}{Name of the time gap_var variable, if used. See \code{\link[=avg_sops]{avg_sops()}}.}

\item{time_covariates}{Optional time-varying covariate lookup table for explicit
precomputed time-basis columns. See \code{\link[=avg_sops]{avg_sops()}}.}

\item{include_re}{Logical. For \code{rmsb::blrm()} fits with \code{cluster()}, include
fitted random-effect draws in posterior predictions for known IDs.}

\item{n_draws}{Integer number of posterior draws to sample for \code{blrm}, or
\code{NULL} to use all stored draws.}

\item{seed}{Optional random seed for reproducible posterior draw sampling.}

\item{posterior_summary}{Summary used for \code{blrm} posterior comparison draws:
\code{"mean"} or \code{"median"}.}

\item{conf_level}{Confidence level for \code{blrm} posterior intervals.}

\item{return_draws}{Logical. For \code{blrm}, store posterior comparison draws.
Frequentist uncertainty draws are stored by \code{\link[=inferences]{inferences()}}.}

\item{...}{Additional arguments reserved for future extensions.}
}
\value{
A data frame of class \code{markov_avg_comparisons}.
}
\description{
Computes average contrasts between counterfactual state occupancy summaries.
The first value in \code{variables[[1]]} is the reference level, and every later
value is compared to it. Reverse the order of values to reverse the contrast.
}
\details{
\code{estimand = "sop"} and \code{estimand = "time_in_state"} are computed from marginal
SOPs. \code{estimand = "time_benefit"} is computed before marginalization from
paired patient/profile-level counterfactual state distributions, because it
is nonlinear in the two state distributions.

Frequentist uncertainty follows the existing package workflow:
\code{avg_comparisons(...) |> inferences(...)}. Bayesian \code{blrm} models use their
posterior SOP draws directly and therefore return uncertainty from
\code{avg_comparisons()}.

For supported frequentist models, \code{inferences(method = "delta")} provides
analytical intervals for differences in SOPs or time in state. The default
\code{vcov = "unconditional"} accounts for coefficient estimation and sampling
the patients used in the averages. Use \code{vcov = "conditional"} to treat
those patients' starting states and covariates as given and account only
for coefficient estimation. User-supplied \code{newdata} requires the conditional
choice. See \code{\link[=inferences]{inferences()}} for model restrictions.
}
\examples{
\dontrun{
avg_comparisons(
  fit,
  variables = list(tx = c(0, 1)),
  estimand = "time_in_state",
  state_sets = "1",
  times = 1:30,
  y_levels = 1:6,
  absorb = 6
) |>
  inferences(method = "mvn", n_draws = 500)
}

}
\seealso{
\code{\link[=avg_sops]{avg_sops()}}, \code{\link[=sops]{sops()}}, \code{\link[=inferences]{inferences()}}
}
