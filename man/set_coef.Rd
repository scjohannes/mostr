% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mvn_helpers.R
\name{set_coef}
\alias{set_coef}
\alias{set_coef.default}
\alias{set_coef.orm}
\alias{set_coef.lrm}
\alias{set_coef.rms}
\alias{set_coef.robcov_vglm}
\alias{set_coef.vglm}
\title{Set Coefficients for Model Objects}
\usage{
set_coef(model, new_coefs)

\method{set_coef}{default}(model, new_coefs)

\method{set_coef}{orm}(model, new_coefs)

\method{set_coef}{lrm}(model, new_coefs)

\method{set_coef}{rms}(model, new_coefs)

\method{set_coef}{robcov_vglm}(model, new_coefs)

\method{set_coef}{vglm}(model, new_coefs)
}
\arguments{
\item{model}{A fitted model object (e.g., \code{vglm}, \code{orm}, \code{lrm}).}

\item{new_coefs}{A numeric vector of coefficients to set. Must match
the length and order of the original coefficients.}
}
\value{
The model object with updated coefficients.
}
\description{
Replaces the coefficients in a fitted model object. This is used for
simulation-based inference (MVN simulation) where we draw coefficient
vectors from a multivariate normal distribution and need to compute
predictions using each draw.
}
\details{
This function creates a shallow copy of the model and replaces its
coefficients. The modified model can then be passed to \code{predict()} or
\code{soprob_markov()} to compute predictions under the simulated coefficients.

\strong{Supported model classes:}
\itemize{
\item \code{orm}, \code{lrm}, \code{rms} (S3): Modifies \verb{$coefficients}
\item \code{vglm} (S4): Modifies \verb{@coefficients} slot
\item \code{robcov_vglm}: Modifies \verb{$coefficients}
}

\strong{Note:} Prediction functions (\code{predict.orm}, \code{predict.vglm}) recompute
linear predictors from the design matrix and coefficients, so we only need
to replace the coefficients - not any stored linear predictors.
}
\examples{
\dontshow{if (rlang::is_installed("rms")) withAutoprint(\{ # examplesIf}
trial <- sim_actt2_markov(n_patients = 40, follow_up_time = 6, seed = 1)
markov_data <- prepare_markov_data(trial, absorbing_state = 8)
fit <- rms::orm(
  y ~ time + tx + yprev,
  data = markov_data,
  x = TRUE,
  y = TRUE,
  opt_method = "LM",
  scale = TRUE
)

new_coefs <- coef(fit)
new_coefs["tx"] <- new_coefs["tx"] - 0.1
fit_shifted <- set_coef(fit, new_coefs)
coef(fit_shifted)["tx"]
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_vcov_robust]{get_vcov_robust()}} for obtaining robust covariance matrices
}
