% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz-transitions.R
\name{plot_transitions}
\alias{plot_transitions}
\title{Plot Empirical or Model-Based Transition Proportions}
\usage{
plot_transitions(
  x,
  newdata = NULL,
  refit_data = NULL,
  variables = NULL,
  times = NULL,
  y_levels = NULL,
  absorb = NULL,
  comparison = c("none", "difference"),
  time_var = "time",
  y_var = "y",
  p_var = "yprev",
  p2_var = NULL,
  id_var = NULL,
  facet_var = NULL,
  gap_var = NULL,
  time_covariates = NULL,
  seed = NULL,
  n_draws = 100L,
  show_values = TRUE,
  digits = NULL,
  fill_limits = NULL
)
}
\arguments{
\item{x}{A trajectory data frame or a fitted Markov model.}

\item{newdata}{Optional data frame of prediction profiles for model-based
plots. If \code{NULL}, wrapper-fitted models use their stored data and extract
one prediction row per ID.}

\item{refit_data}{Optional full longitudinal data used only for stored-data
resolution in model-based plots.}

\item{variables}{Optional named list with one counterfactual variable. For
\code{comparison = "difference"}, at least two values are required and the
first value is the reference.}

\item{times}{Optional current-time values to plot. Required for model-based
plots.}

\item{y_levels}{Optional state levels. If omitted for model-based plots,
levels are inferred from the model when possible.}

\item{absorb}{Optional absorbing-state label.}

\item{comparison}{\code{"none"} for transition proportions or \code{"difference"} for
counterfactual differences in transition proportions.}

\item{time_var}{Character name of the time column in trajectory data.}

\item{y_var}{Character name of the current-state column in trajectory data.}

\item{p_var}{Character name of the previous-state column.}

\item{p2_var}{Optional second previous-state column for second-order
model recursion.}

\item{id_var}{Optional ID column used to extract one stored prediction row per
patient.}

\item{facet_var}{Optional observed grouping variable. The plot always facets
by time; \code{facet_var} adds grouping to the time panels.}

\item{gap_var}{Optional time-gap_var variable used by the fitted model.}

\item{time_covariates}{Optional time-varying covariate lookup table used by the fitted
model.}

\item{seed}{Optional random seed for \code{blrm} posterior draw selection.}

\item{n_draws}{Integer number of posterior draws to sample for \code{blrm}, or
\code{NULL} to use all stored draws. Defaults to 100.}

\item{show_values}{Logical. If \code{TRUE}, print rounded values in each tile.}

\item{digits}{Number of digits used for tile labels. Defaults to 2 for
proportions and 3 for differences.}

\item{fill_limits}{Optional fill-scale limits. Difference plots use symmetric
limits around zero when this is \code{NULL}.}
}
\value{
A ggplot object.
}
\description{
\code{plot_transitions()} creates heatmaps of population transition proportions
from observed trajectory data or from transition probabilities implied by a
fitted Markov model. Model-based plots are population averaged over the
supplied or stored patient profiles. When \code{comparison = "difference"},
counterfactual transition summaries are computed for the levels in
\code{variables}, and the heatmap shows the difference in transition proportions
relative to the first level.
}
\details{
Minimum inputs depend on the object type. For trajectory data, \code{object} must
contain the current time, current state, and previous-state columns named by
\code{time_var}, \code{y_var}, and \code{p_var}. For model-based plots, pass a fitted
Markov model and \code{times}; use \code{newdata} for explicit prediction profiles and
pass \code{y_levels} or \code{absorb} when they are not inferable or absorbing-state
behavior matters. Difference plots also require
\code{comparison = "difference"} and \code{variables = list(var = c(reference, comparison))}.
}
\examples{
\dontrun{
plot_transitions(data, time_var = "week", p_var = "yprev")

plot_transitions(
  fit,
  times = 1:7,
  variables = list(tx = c(0, 1)),
  comparison = "difference",
  y_levels = 1:8,
  absorb = 8
)
}
}
