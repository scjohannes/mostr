% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-markov.R
\name{sim_actt2_markov}
\alias{sim_actt2_markov}
\title{Simulate ACTT-2 Markov Trajectories}
\usage{
sim_actt2_markov(
  n_patients = 1000,
  treatment_prob = 0.5,
  treatment_effect = -0.0657,
  treatment_effect_decay = 0,
  seed = NULL,
  ...
)
}
\arguments{
\item{n_patients}{Integer. Number of patients to simulate (default: 1000).}

\item{treatment_prob}{Numeric. Probability of assignment to Baricitinib plus
Remdesivir, represented by \code{tx = 1} (default: 0.5). Placebo plus Remdesivir
is represented by \code{tx = 0}.}

\item{treatment_effect}{Numeric. Day-1 log-odds coefficient for Baricitinib
plus Remdesivir (\code{tx = 1}) relative to Placebo plus Remdesivir (\code{tx = 0}).
The default -0.0657 is the active-arm equivalent of the reported reverse
contrast.}

\item{treatment_effect_decay}{Nonnegative exponential decay rate per day.
The effect on day \code{t} is \code{treatment_effect * exp(-treatment_effect_decay * (t - 1))}. The default 0 gives a constant
effect.}

\item{seed}{Integer. Random seed for reproducibility (default: \code{NULL}).}

\item{...}{Additional arguments passed to \code{\link[=sim_trajectories_markov]{sim_trajectories_markov()}}. These
can override ACTT-2 defaults such as \code{follow_up_time}. Overrides are applied
after the wrapper generates its baseline data; supplying \code{baseline_data}
replaces those generated baseline rows.}
}
\value{
A data frame with the same columns returned by
\code{\link[=sim_trajectories_markov]{sim_trajectories_markov()}}. The default output contains 28 rows per
patient and states numbered 1 through 8.
}
\description{
Generates ACTT-2-like eight-state ordinal trajectories from a fitted
proportional-odds transition model. The wrapper samples baseline states and
treatment assignments, evaluates the model's restricted cubic spline in day,
and delegates trajectory generation to \code{\link[=sim_trajectories_markov]{sim_trajectories_markov()}}.
}
\details{
Initial previous states are sampled from states 4 through 7 with
probabilities 0.1374637, 0.5459826, 0.2090997, and 0.1074540. They represent
the states immediately before the first generated day-1 outcome. State 8 is
absorbing by default. Set \code{follow_up_time = 60} for 60 days; days beyond
28 extrapolate the fitted time effect.

The transition model uses the reported \code{rms::orm()} thresholds for \code{y >= 2}
through \code{y >= 8}, reversed to match the cumulative-probability ordering used
by \code{\link[=sim_trajectories_markov]{sim_trajectories_markov()}}. Shared previous-state and spline coefficients
retain their reported order and signs. The treatment effect is supplied
separately through \code{treatment_effect}. The reported coefficients are rounded,
so this function reproduces the supplied model summary rather than an
unavailable full-precision fit.
}
\examples{
actt2 <- sim_actt2_markov(n_patients = 20, seed = 123)
head(actt2)
decaying <- sim_actt2_markov(
  n_patients = 20,
  treatment_effect = log(0.8),
  treatment_effect_decay = -log(0.01) / 14,
  seed = 123
)

}
