% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-markov.R
\name{sim_actt1_markov}
\alias{sim_actt1_markov}
\title{Simulate ACTT-1 ordinal patient trajectories}
\usage{
sim_actt1_markov(
  n_patients = 1000,
  treatment_prob = 0.5,
  treatment_effect = -0.0657,
  treatment_effect_decay = 0,
  seed = NULL,
  ...
)
}
\arguments{
\item{n_patients}{Number of patients to simulate. Must be a positive integer.}

\item{treatment_prob}{Probability of assignment to Remdesivir. Must be a
numeric scalar between 0 and 1.}

\item{treatment_effect}{Day-1 proportional-odds coefficient applied to
Remdesivir (\code{tx = 1}) relative to placebo (\code{tx = 0}). The default -0.0657
matches \code{\link[=sim_actt2_markov]{sim_actt2_markov()}}.}

\item{treatment_effect_decay}{Nonnegative exponential decay rate per day.
The effect on day \code{t} is \code{treatment_effect * exp(-treatment_effect_decay * (t - 1))}. The default 0 gives a constant
effect.}

\item{seed}{Optional random-number seed. The seed is set once before baseline
states and treatment assignments are sampled, and trajectory generation
continues from the same random-number stream.}

\item{...}{Advanced overrides passed to \code{\link[=sim_trajectories_markov]{sim_trajectories_markov()}}. These
can override defaults such as \code{follow_up_time}. Overrides are applied after
the wrapper generates its baseline data; supplying \code{baseline_data} replaces
those generated baseline rows.}
}
\value{
A data frame with the same columns returned by
\code{\link[=sim_trajectories_markov]{sim_trajectories_markov()}}. The default output contains 28 rows per
patient and states numbered 1 through 8.
}
\description{
Simulates individual 8-state ACTT-1 trajectories from a simplified Bayesian
Markov proportional-odds model. The wrapper uses posterior mean coefficients,
the reported baseline-state counts, and the ACTT-2 restricted cubic spline
knots because the original ACTT-1 knot locations are unavailable.
}
\details{
Initial previous states are sampled from states 4 through 7 according to the
reported counts 138, 435, 193, and 285. These counts are normalized over the
1,051 patients with a reported state and represent states immediately before
the first generated day-1 outcome. State 8 is absorbing by default.

Treatment is coded as \code{tx = 0} for placebo and \code{tx = 1} for Remdesivir. The
user-specified effect replaces the fitted ACTT-1 treatment main effect and
treatment-by-day spline interactions. Age and sex are omitted because the
required joint baseline distribution is unavailable or the fitted effect was
judged negligible.

The fitted constrained partial proportional-odds time deviation is retained.
For the cumulative cutoff \code{y >= k}, it adds \code{-0.0188 * day * k} to the linear
predictor. This threshold-specific term is essential for reproducing ACTT-1
mortality over follow-up even though its printed coefficient is small.

The reported thresholds for \code{y >= 2} through \code{y >= 8} are reversed only when
passed to \code{\link[=sim_trajectories_markov]{sim_trajectories_markov()}}; the shared previous-state and time
coefficients retain their reported signs. Coefficients are rounded, so this
function reproduces the supplied model summary rather than an unavailable
full-precision fit.
}
\examples{
actt1 <- sim_actt1_markov(n_patients = 20, seed = 123)
head(actt1)
decaying <- sim_actt1_markov(
  n_patients = 20,
  treatment_effect = log(0.8),
  treatment_effect_decay = -log(0.01) / 14,
  seed = 123
)

}
