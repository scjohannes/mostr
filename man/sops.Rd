% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sops-api.R
\name{sops}
\alias{sops}
\title{Calculate Individual State Occupation Probabilities}
\usage{
sops(
  model,
  newdata = NULL,
  times,
  y_levels = NULL,
  absorb = NULL,
  by = NULL,
  refit_data = NULL,
  id_var = NULL,
  time_var = "time",
  p_var = "yprev",
  p2_var = NULL,
  gap_var = NULL,
  time_covariates = NULL,
  include_re = FALSE,
  n_draws = 100L,
  seed = NULL,
  posterior_summary = c("mean", "median"),
  conf_level = 0.95,
  return_draws = FALSE,
  ...
)
}
\arguments{
\item{model}{A fitted model object (e.g., \code{vglm}, \code{orm}, or \code{blrm}). For
\code{vglm} models, the family must be cumulative-logit with \code{reverse = TRUE}.
For \code{orm} models, the family must be logistic.}

\item{newdata}{Optional. A data frame of prediction profiles. When supplied,
every row supplies a separate starting state and set of covariates. The
internal \code{rowid} column is regenerated. If \code{NULL}, uses the data stored by
\code{\link[=orm_markov]{orm_markov()}}, \code{\link[=blrm_markov]{blrm_markov()}}, or \code{\link[=vglm_markov]{vglm_markov()}} and requires exactly
one complete designated starting profile per fitted patient. \code{refit_data}
is never used as a prediction-profile fallback.}

\item{times}{Required visit-scale time points to estimate. For numeric time
variables this is usually a numeric vector. For factor-valued visit
indices, values are matched to fitted visit levels.}

\item{y_levels}{A vector of state levels in fitted outcome order. Its length
must agree with the fitted model's threshold count. If \code{NULL}, attempts to
infer the levels from \code{model}.}

\item{absorb}{The absorbing state.}

\item{by}{Optional character vector of variable names to stratify by. When
provided, the function aggregates (averages) SOPs within each stratum
defined by combinations of these variables. E.g., \code{by = "ecog"} aggregates
within ECOG levels; \code{by = c("ecog", "age_group")} aggregates within each
combination of ECOG and age group. Rows missing any grouping value are
omitted; an error is raised if none remain. This is simple aggregation
within observed strata, not G-computation standardization (use
\code{avg_sops()} for that).}

\item{refit_data}{Optional full longitudinal data used only by refit-bootstrap
inference. It is not used for point estimates. Defaults to data stored on
wrapper-fitted models.}

\item{id_var}{Character ID column used when \code{include_re = TRUE}. For \code{blrm},
\code{NULL} is inferred from wrapper metadata, then \code{model$clusterInfo$name}
when available, otherwise \code{"id"}. For frequentist models, \code{id_var} is used
for stored starting-profile validation and refit bootstrap metadata, not
for ordinary user-supplied prediction rows.}

\item{time_var}{Name of the time variable in the model.}

\item{p_var}{Name of the previous state variable in the model.}

\item{p2_var}{Optional second previous-state variable. \code{NULL} uses a
first-order Markov recursion; a non-\code{NULL} column name uses a second-order
recursion.}

\item{gap_var}{Name of the time gap_var variable (if used).}

\item{time_covariates}{Optional time-varying covariate lookup table for
explicit basis columns. Registered inline terms such as
\code{rms::rcs(time, 4)} and \code{rms::lsp(time, c(3, 7))} can be used without
supplying \code{time_covariates}.}

\item{include_re}{Logical. For \code{rmsb::blrm()} fits with \code{cluster()}, include
fitted random-effect draws in posterior predictions for known IDs.}

\item{n_draws}{Integer number of posterior draws to sample for \code{blrm}, or
\code{NULL} to use all stored draws. Defaults to 100.}

\item{seed}{Optional random seed for reproducible random draw sampling.}

\item{posterior_summary}{Summary used for \code{blrm} posterior SOP draws:
\code{"mean"} or \code{"median"}.}

\item{conf_level}{Confidence level for \code{blrm} posterior intervals.}

\item{return_draws}{Logical. For \code{blrm}, store posterior SOP draws for
extraction with \code{get_draws()}. Large draw objects are guarded to protect
memory.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A data frame of class \code{markov_sops} containing:
\item{rowid}{Row identifier from newdata (omitted if \code{by} is specified)}
\item{time}{Time point}
\item{state}{State name}
\item{estimate}{Probability of being in the state (individual-level or stratum average)}
\item{conf.low, conf.high, std.error}{For \code{blrm} fits, posterior
uncertainty summaries computed directly from SOP draws}
\item{(by variables)}{Stratification variables if \code{by} is specified}
Plus all columns from \code{newdata} (for individual-level results).
}
\description{
Computes individual-level state occupation probabilities (SOPs) for each row
in a dataset. Optionally aggregates results within strata defined by grouping
variables.
}
\details{
For supported frequentist models, pass the result to
\code{inferences(method = "delta", vcov = "conditional")} for analytical
confidence intervals. These account for coefficient estimation while
treating the starting states and covariates used for prediction as given.
\code{sops()} does not support unconditional analytical variance. See \code{\link[=inferences]{inferences()}}
for model restrictions and other inference methods.

This function wraps \code{soprob_markov()} and converts its array output to a tidy
data frame. The output contains one row per patient-time-state combination
(or stratum-time-state if \code{by} is used).

Automatic prediction from a wrapper-fitted model uses the cohort-wide
designated \code{first_followup_time} retained before response-driven model-frame
omission. The transition response is not required on that row, but ID,
modeled predictors, time information, and previous state must be complete.
Each included patient must contribute at least one usable fitted transition
somewhere; later rows are not substituted for a missing starting profile.

For \code{rmsb::blrm()} models, SOPs are computed on sampled posterior draws and
then summarized. The point estimate is the requested posterior summary of the
SOP draws, not a plug-in calculation from summarized model parameters.
State-wise medians and interval bounds are not constrained to sum to one
across states; use \code{posterior_summary = "mean"} when the displayed estimates
themselves need to preserve total probability. Draw-level probabilities
stored with \code{return_draws = TRUE} remain normalized within each draw.

\strong{Model Requirements:}

For \code{vglm} models, only the cumulative-logit family with \code{reverse = TRUE} is
supported. For \code{orm} models, only the logistic family is supported. This is
because the package's Markov simulation logic expects
higher-numbered states to represent worse outcomes, and uses reverse cumulative
probabilities to model the probability of being in state k or worse.

\strong{Stratification:}

When \code{by} is specified, the function:
\enumerate{
\item Computes individual-level SOPs for all patients
\item Groups results by time, state, and the specified variables
\item Averages the SOPs within each group
}

This aggregation preserves heterogeneity across observed strata without
creating counterfactual scenarios (unlike G-computation in \code{avg_sops()}).

For computing marginal/standardized SOPs (G-computation), use \code{avg_sops()}
instead, which creates counterfactual datasets and averages over individuals.
}
\examples{
\dontrun{
# Individual-level SOPs
sops_ind <- sops(fit, newdata = baseline_data, times = 1:30, y_levels = 1:6)

# Stratified by ECOG performance status
sops_ecog <- sops(fit, newdata = baseline_data, times = 1:30, y_levels = 1:6,
                  by = "ecog")

# Stratified by multiple variables
sops_strat <- sops(fit, newdata = baseline_data, times = 1:30, y_levels = 1:6,
                   by = c("ecog", "age_group"))

# Compatible with inferences()
sops_strat |> inferences(method = "mvn", n_draws = 1000)
}

}
\seealso{
\code{\link[=avg_sops]{avg_sops()}} for marginal SOPs, \code{\link[=soprob_markov]{soprob_markov()}} for the underlying
computation.
}
