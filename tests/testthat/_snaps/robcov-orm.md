# orm bread validation rejects nonpositive and singular matrices

    Code
      orm_model_bread(fit)
    Condition
      Error in `orm_model_bread()`:
      ! `orm model bread` must be positive definite.

---

    Code
      orm_model_bread(fit)
    Condition
      Error in `orm_model_bread()`:
      ! `orm model bread` must be positive definite.

---

    Code
      orm_model_bread(fit)
    Condition
      Error in `orm_model_bread()`:
      ! `orm model bread` must be positive definite.

---

    Code
      orm_model_bread(fit)
    Condition
      Error in `orm_model_bread()`:
      ! `orm model bread` must be positive definite.

